# Changelog

## 0.1.0 - 2026-08-14

### Features

- Initial release (PRD 0022 FR-002, plan 0036 P0-09). Puck-native wrapper
  over `@anvilkit/ui` `Badge`: `label` (text, inline-text editable) and
  `variant` (the exact 6-value `badgeVariants` cva union). Single `root`
  style target; base-ui's `render` prop is deliberately not exposed.
