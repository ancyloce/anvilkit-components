# Changelog

## 0.1.0 - 2026-08-14

### Features

- Initial release (PRD 0022 FR-002, plan 0036 P0-10). Puck-native wrapper
  over `@anvilkit/ui` `Separator` with a single `orientation` field
  (`horizontal` | `vertical`). One `root` style target; no slots.
